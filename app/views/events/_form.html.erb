<%= form_with(model: @event, local: true, url: events_path, method: 'post') do |form|%>
  <% if @event.errors.any? %>
    <div class="form-group">

    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% @event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    </div>
  <% end %>

  <%
=begin%>
 <p>
  <strong> eid:</strong>
  <%= ticket.event_id %>
  </p> 
<%
=end%>
  <div class="form-group">
  <div class="field">
    <%= form.label :artist %>
    <%= form.text_field :artist, class: 'form-control' %>
  </div>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, class: 'form-control' %>
  </div>

    <div class="field">
    <%= form.label :price_low %>
    <%= form.text_field :price_low, class: 'form-control' %>
  </div>

    <div class="field">
    <%= form.label :price_high %>
    <%= form.text_field :price_high, class: 'form-control' %>
  </div>

      <div class="field">
    <%= form.label :event_date %>
<%= form.date_field :event_date, class: 'form-control' %>

  </div>
</div>

  <div class="actions">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

    <%= form.submit class: 'btn btn-primary text-light'%>
  </div>
<% end %>
